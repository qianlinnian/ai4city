name: ai4city-mas
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - langgraph>=0.3,<0.4
      - pydantic>=2.10,<3
      - PyYAML>=6,<7
      - rich>=13,<16
      - typer>=0.16,<1

# Heavy model, VR, physiology, and Rhino/Grasshopper dependencies are adapters,
# not backbone requirements. Install them only for the deployment that uses them.
